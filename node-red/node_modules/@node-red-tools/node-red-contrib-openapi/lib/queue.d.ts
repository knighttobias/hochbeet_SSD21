import { Handler, NextFunction, Request, Response } from 'express';
export declare type RequestID = string;
export declare function enqueue(req: Request, res: Response, next: NextFunction): RequestID;
export declare function dequeue(id: RequestID, handler: Handler): void;
