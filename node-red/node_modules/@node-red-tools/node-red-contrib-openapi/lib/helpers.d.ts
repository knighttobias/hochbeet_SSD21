import { Red } from 'node-red';
import { ConfigSchema, LazyRouter } from './models';
export declare function findSchema(RED: Red, configId: string): ConfigSchema | undefined;
export declare function findRouter(RED: Red, configId: string): LazyRouter | undefined;
